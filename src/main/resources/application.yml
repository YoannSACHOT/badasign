spring:
  application:
    name: badasign

server:
  port: 58082

yousign:
  api:
    base-url: https://api.yousign.app/v3
    api-key: ${YOUSIGN_API_KEY:my_key}
    
logging:
  level:
    fr.jixter.badasign: DEBUG
    root: INFO